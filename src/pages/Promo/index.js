import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import { HeaderPromo } from '../../components'

export default function Promo({navigation}) {
    return (
        <View>
            <HeaderPromo/>
        </View>
    )
}

const styles = StyleSheet.create({})
